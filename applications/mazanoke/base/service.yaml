apiVersion: v1
kind: Service
metadata:
  name: mazanoke
  namespace: mazanoke
  labels:
    app: mazanoke
    kubernetes.io/name: mazanoke
spec:
  type: ClusterIP
  selector:
    app: mazanoke
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
