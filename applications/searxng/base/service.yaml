apiVersion: v1
kind: Service
metadata:
  name: searxng
  namespace: searxng
  labels:
    app: searxng
    kubernetes.io/name: searxng
spec:
  type: ClusterIP
  selector:
    app: searxng
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
