# https://artifacthub.io/packages/helm/argo/argo-cd/7.9.0?modal=values

global:
  domain: argocd.okwilkins.dev
controller:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
server:
  config:
    url: https://argocd.okwilkins.dev
  # As HTTPS is dealt with with Cloudflared, internally use HTTP
  extraArgs:
    - --insecure
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
repoServer:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
applicationSet:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
redis:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
dex:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
notifications:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
