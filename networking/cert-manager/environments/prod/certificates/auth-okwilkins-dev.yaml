apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-okwilkins-dev
  namespace: authelia
spec:
  secretName: auth-okwilkins-dev-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: auth.okwilkins.dev
  dnsNames:
    - auth.okwilkins.dev
