apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: search-okwilkins-dev
  namespace: searxng
spec:
  secretName: search-okwilkins-dev-tls
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: search.okwilkins.dev
  dnsNames:
    - search.okwilkins.dev
