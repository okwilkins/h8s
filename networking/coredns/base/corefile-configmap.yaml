apiVersion: v1
kind: ConfigMap
metadata:
  name: corefile-coredns
  namespace: coredns-lan
data:
  Corefile: |-
    .:53 {
        errors
        health {
            lameduck 5s
        }
        ready
        log . {
            class error
        }
        prometheus :9153

        hosts /blocklist/hosts {
          fallthrough
        }
        forward . 1.1.1.1 1.0.0.1
        loop
        reload
        loadbalance
    }
