apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: external-secrets-operator-dashboard
spec:
  configMapRef:
    name: external-secrets-dashboard
    key: external-secrets.json
  folderRef: external-secrets-operator
