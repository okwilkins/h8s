apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authelia-argocd-client-secret
  namespace: authelia
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: vault-secretstore
    kind: ClusterSecretStore
  target:
    name: authelia-argocd-client-secret
    creationPolicy: Orphan
  data:
    - secretKey: client-secret-hash
      remoteRef:
        key: authelia/argocd-oidc
        property: client-secret-hash
